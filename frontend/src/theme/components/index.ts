import { Button } from './Button'
import { Form } from './Form'
import { FormError } from './FormError'
import { Input } from './Input'
import { Tabs } from './Tabs'

export const components = { Button, Input, FormError, Form, Tabs }
